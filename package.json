{
  "name": "clean-architecture",
  "version": "1.0.0",
  "description": "Implémentation d'une clean architecture de A à Z !",
  "main": "index.js",
  "scripts": {
    "build": "tsc --noEmit ./index.ts && esbuild index.ts --bundle --platform=node --format=cjs --outfile=dist/index.js"
  },
  "bin": {
    "tweet": "dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.3.0",
    "esbuild": "^0.18.0",
    "typescript": "^5.1.3"
  }
}
